Static, rarely-changing brand assets (e.g. a hand-designed logo file) can go
here and be referenced as `/images/<file>`.

Product photos/videos should NOT go here — upload them through
Admin → Products, which stores them in Supabase Storage
(`product-media` bucket) so they're manageable without a code deploy.
